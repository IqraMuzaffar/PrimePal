// Feature 10: Individual student analytics — per-pillar competency breakdown
export default function AnalyticsPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold mb-6">Student Analytics</h1>
      {/* TODO: student selector, fetch GET /api/v1/evaluator/report/student/:id */}
      {/* Render: Reading / Writing / Listening / Speaking score charts */}
    </div>
  );
}
