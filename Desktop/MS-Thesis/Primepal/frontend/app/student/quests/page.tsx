// Feature 5 & 6: Four-Pillar Quest UI — weekly missions
export default function QuestsPage() {
  return (
    <div>
      <h1 className="text-xl font-bold mb-4">Your Weekly Quest</h1>
      {/* TODO: fetch GET /api/v1/tutor/quest/:studentId/current */}
      {/* Render four task cards: Reading, Listening (audio player), Writing, Speaking (hold-to-speak mic) */}
    </div>
  );
}
